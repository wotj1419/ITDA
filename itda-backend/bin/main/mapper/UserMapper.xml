<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itda.backend.auth.repository.UserMapper">

    <!-- Result Map -->
    <resultMap id="UserResultMap" type="com.itda.backend.auth.domain.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="name" column="name"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="role" column="role" javaType="com.itda.backend.auth.domain.UserRole"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- ID로 사용자 조회 -->
    <select id="findById" resultMap="UserResultMap">
        SELECT id, email, password_hash, name, profile_image_url, role, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 이메일로 사용자 조회 -->
    <select id="findByEmail" resultMap="UserResultMap">
        SELECT id, email, password_hash, name, profile_image_url, role, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 사용자 등록 -->
    <insert id="insertUser" parameterType="com.itda.backend.auth.domain.User"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO users (email, password_hash, name, profile_image_url, role)
        VALUES (#{email}, #{passwordHash}, #{name}, #{profileImageUrl}, #{role})
    </insert>

    <!-- 프로필 정보 업데이트 -->
    <update id="updateProfile">
        UPDATE users
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="profileImageUrl != null">
                profile_image_url = #{profileImageUrl},
            </if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <!-- 비밀번호 변경 -->
    <update id="updatePassword">
        UPDATE users
        SET password_hash = #{passwordHash},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
