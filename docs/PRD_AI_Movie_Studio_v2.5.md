# 🎬 AI Movie Studio - Product Requirements Document (PRD)

> **버전**: 2.5.3  
> **최종 수정일**: 2026-01-15  
> **프로젝트 기간**: 6주  

---

## 목차

- 1. 개요 (Overview)
- 2. 문제 정의 (Problem Statement)
- 3. 타겟 사용자 (Target Users)
- 4. 핵심 기능 (Core Features)
- 5. 사용자 플로우 (User Flow)
- 6. 화면 구성
- 7. 기술 요구사항
- 8. API 명세 (초안)
- 9. 개발 일정 (6주)
- 10. 리스크 및 완화 방안
- 11. 성공 지표
- 12. 부록
- 13. 문서 이력

## 1. 개요 (Overview)

### 1.1 프로젝트 소개
**AI Movie Studio**는 AI 영화 제작자들이 **기획부터 완성까지 전체 워크플로우**를 하나의 플랫폼에서 진행할 수 있는 AI 영화 제작 스튜디오입니다.

> 한 줄 요약: **"아이디어만 있으면, AI가 당신의 영화를 만들어 드립니다"**

사용자는 시나리오 구상 → 스토리보드 이미지 생성 → AI 영상 변환 → 음악 추가 → 영상 편집의 전 과정을 AI 도구들과 함께 진행하며, WebRTC 기반 협업 기능으로 팀원들과 실시간으로 아이디어를 나눌 수 있습니다.

### 1.2 핵심 가치 제안 (Value Proposition)

| 가치 | 설명 |
|------|------|
| 🎭 **올인원 워크플로우** | 기획-스토리보드-영상생성-편집까지 하나의 플랫폼에서 |
| 🤖 **AI 파워 통합** | Gemini(이미지), Google Veo 3.1(영상) 등 Google Cloud AI 통합 |
| 👥 **실시간 협업** | WebRTC 화상통화로 팀원과 아이디어 공유 및 브레인스토밍 |
| 🎬 **프로젝트 관리** | 씬 단위로 체계적인 영화 제작 프로세스 관리 |
| ⚡ **빠른 프로토타이핑** | 아이디어를 1분 내외의 AI 영화로 빠르게 구현 |

### 1.3 핵심 워크플로우

```
┌─────────────────────────────────────────────────────────────────┐
│ 1️⃣ 기획/시나리오 설계                                            │
│    • WebRTC 화상통화로 팀원과 브레인스토밍                        │
│    • AI 시나리오 생성 도구로 스토리 아이디어 구체화                │
│    • 프로젝트 생성 및 씬 구조 설계                                │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2️⃣ 스토리보드 생성                                               │
│    • Gemini API로 각 씬의 스토리보드 이미지 생성                  │
│    • 마스터 샷 → 다양한 앵글(클로즈업, 오버더숄더 등) 생성        │
│    • 필요시 프롬프트 수정으로 이미지 정교화                       │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3️⃣ AI 영상 생성                                                  │
│    • 스토리보드 이미지 → AI 영상 변환 (Image-to-Video)            │
│    • Google Veo 3.1 API (P0), 오픈소스 모델 선택 옵션 (P1)        │
│    • 카메라 움직임, 분위기 등 프롬프트로 조절                     │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4️⃣ 음악 파일 업로드/보관                                         │
│    • 외부에서 생성한 배경음악 파일 업로드 (P1, 저장용)            │
│    • 편집/믹싱은 P2에서 검토                                      │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5️⃣ 영상 편집/조합                                                │
│    • 씬별 영상 클립들을 순서대로 병합                             │
│    • 필요시 분할/트리밍으로 구간 조절                             │
│    • 자막 추가 (선택)                                             │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 🎬 완성된 AI 영화                                                 │
│    • 미리보기 및 다운로드                                         │
└─────────────────────────────────────────────────────────────────┘
```
> 범위 메모: **P0는 배경음악/효과음 없이** 씬별 확정 영상 병합까지를 목표로 하고, **P1은 음악 업로드/보관**, **P2에서 믹싱/병합**을 검토합니다.


### 1.4 기술 스택

| 영역 | 기술 |
|------|------|
| Frontend | Vue.js 3 (Composition API), Vue Flow (노드 기반 에디터) |
| Backend | Spring Boot 3.x |
| 실시간 통신 | WebRTC (시그널링 WebSocket) + 프로젝트 이벤트 WebSocket(Job 완료/실패 알림) |
| 실시간 동기화 | Yjs / CRDT (P1, 피그마 스타일 협업) |
| AI 이미지 생성 | Google Gemini API (Gemini 2.5 Pro Image) |
| AI 영상 생성 | Google Veo 3.1 API (P0), 오픈소스 모델 (P1: Stable Video Diffusion 등) |
| 음악/효과음 | 외부 파일 업로드/보관 (편집/믹싱은 향후 검토) |
| 영상 편집/병합 | Server-side FFmpeg (Worker 비동기 Job) |
| 데이터베이스 | MySQL (MVP 기준) |
| 파일 저장소 | AWS S3 (MVP 기준, 로컬 개발 시 MinIO 대체 가능) |
| 메시지 큐 | Redis Streams (비동기 작업 처리) |
| 캐싱 | Redis (세션, API 응답 캐싱) |
| 웹서버/리버스 프록시 | Nginx |
| CI/CD | Jenkins |
| 모니터링/로깅 | Prometheus + Grafana (또는 CloudWatch) |
| API 문서화 | Swagger / OpenAPI |
| 인프라 | AWS EC2 / Docker |
| 컨테이너 레지스트리 | Docker Hub (또는 AWS ECR) |

---

## 2. 문제 정의 (Problem Statement)

### 2.1 현재 상황
- AI 영상 생성 도구(Runway, Luma 등)는 강력하지만, **각각 별도로 사용**해야 함
- 시나리오 → 이미지 → 영상 → 편집까지 **여러 도구를 오가며 작업**해야 하는 번거로움
- 팀 협업 시 아이디어 공유와 피드백이 분산되어 **커뮤니케이션 비용** 발생
- 기존 영상 편집 도구는 전문적이지만 **학습 곡선이 높음**

### 2.2 해결 목표
- **올인원 플랫폼**: 기획부터 완성까지 하나의 서비스에서 진행
- **AI 통합**: Google Cloud AI (Gemini, Veo 3.1) + 오픈소스 모델을 한 곳에서 활용
- **협업 지원**: WebRTC 화상통화로 실시간 브레인스토밍
- **간편한 편집**: 전문 지식 없이도 클립 병합/조합 가능

---

## 3. 타겟 사용자 (Target Users)

### 3.1 주요 페르소나

#### 페르소나 1: AI 영화 제작자 (Creator)
- **특징**: AI 도구에 관심이 많은 콘텐츠 크리에이터
- **니즈**: 아이디어를 빠르게 영상으로 구현하고 싶음
- **Pain Point**: 여러 AI 도구를 오가며 작업하는 것이 번거로움

#### 페르소나 2: 인디 영화 감독 (Director)
- **특징**: 저예산으로 창작 활동을 하는 영화 감독
- **니즈**: 비용 없이 영화 컨셉/프로토타입을 제작하고 싶음
- **Pain Point**: 실사 촬영 비용과 인력 확보가 어려움

#### 페르소나 3: 콘텐츠 팀 (Team)
- **특징**: 2-3명이 함께 영상 콘텐츠를 제작하는 팀
- **니즈**: 원격으로 아이디어를 공유하고 함께 작업하고 싶음
- **Pain Point**: 기획 단계에서 의견 조율과 피드백이 어려움

---

## 4. 핵심 기능 (Core Features)

### 4.1 기능 우선순위

| 우선순위 | 기능 | 설명 | MVP |
|----------|------|------|-----|
| **P0** | 사용자 인증 | 회원가입, 로그인 (이메일/비밀번호) | ✅ |
| **P1** | OAuth 소셜 로그인 | Google 등 소셜 로그인 지원 | ⬜ |
| **P0** | 프로젝트 관리 | 프로젝트 생성, 씬 관리 | ✅ |
| **P0** | 오브젝트 시트 생성 | 주요 오브젝트(캐릭터, 소품 등) 레퍼런스 이미지 생성 (일관성 유지용) | ✅ |
| **P0** | AI 시나리오 생성 | 키워드 → 시나리오 아이디어 제안 | ✅ |
| **P0** | 협업 (플로팅 바) | 화상통화 + 화면공유, 화면 이동해도 통화 유지 | ✅ |
| **P0** | Presence 표시 | 팀원이 어느 화면에서 작업 중인지 실시간 표시 (WebSocket) | ✅ |
| **P0** | 노드 기반 씬 편집 | Vue Flow 기반 노드 캔버스로 마스터→샷→영상 흐름 시각화 | ✅ |
| **P0** | 마스터 이미지 생성 | 씬의 기준점이 되는 와이드 샷 이미지 생성 + AI 프롬프트 개선 | ✅ |
| **P0** | 스토리보드 그리드 생성 | 마스터 기반 다양한 샷(OTS, 클로즈업 등)을 그리드 이미지로 생성 | ✅ |
| **P0** | 샷 추출 (AI 재생성) | 그리드에서 선택한 샷을 AI로 고품질 재생성 | ✅ |
| **P0** | AI 영상 생성 (Veo 3.1) | Image-to-Video + 카메라 움직임 예시 선택 (Provider: Google Veo 3.1 고정) | ✅ |
| **P1** | AI 영상 생성 (오픈소스) | Stable Video Diffusion 등 오픈소스 모델 선택 옵션 | ⬜ |
| **P0** | 영상 병합 | 씬별 영상을 하나로 조합 (확정된 영상 기준) | ✅ |
| **P0** | 순차 재생 | 씬 내 클립 / 프로젝트 전체 영상 순차 자동 재생 | ✅ |
| **P0** | 진행률 표시 | AI 생성 **상태(대기/진행중/완료/실패)** + 완료/실패 알림 표시 (job status: PENDING/RUNNING/SUCCEEDED/FAILED, 실제 %는 미보장) | ✅ |
| **P1** | 외부 파일 업로드 | 이미지/영상/음악 직접 업로드 | ⬜ |
| **P1** | 퀵 프리뷰 | 프로젝트 상세에서 씬 연결 미리보기 | ⬜ |
| **P1** | 실시간 알림 | 협업 중 팀원 편집 알림 | ⬜ |
| **P1** | 영상 분할/트리밍 | 원하는 구간만 잘라내기 | ⬜ |
| **P1 🔥** | 실시간 동시 편집 | Yjs/CRDT 기반 피그마 스타일 실시간 협업 | ⬜ |
| **P2** | AI 음악 생성 | AI 음악 API 연동 (Suno 등, API 가용성 확인 필요) | ⬜ |
| **P2** | 자막 추가 | 영상에 텍스트 오버레이 | ⬜ |

### 4.2 상세 기능 명세

#### 4.2.1 사용자 인증
```
- 회원가입 (이메일/비밀번호)
- 로그인/로그아웃
- 프로필 관리 (이름, 프로필 이미지)
- 비밀번호 재설정 (이메일 링크/코드 기반)
- Access Token(1일) + Refresh Token(7일) 기반 인증
- Refresh Token은 Redis 저장 + Rotation 적용
- [P1] OAuth 소셜 로그인 (Google 등)
```

#### 4.2.2 프로젝트 관리
```
프로젝트 구조:
📁 프로젝트 (AI 영화 1편)
   ├── 📝 시나리오 (전체 스토리 개요)
   ├── 🎨 오브젝트 시트 (주요 오브젝트 레퍼런스 이미지: 캐릭터, 소품 등)
   └── 🎬 씬 목록
        ├── 씬 1
        │     ├── 설명 (씬 내용)
        │     ├── 등장 오브젝트 (오브젝트 시트에서 선택)
        │     ├── 스토리보드 이미지들
        │     ├── AI 생성 영상
        │     └── 음악/SFX
        ├── 씬 2
        └── ...

기능:
- 프로젝트 생성 (제목, 설명, 장르)
- 프로젝트 목록 조회
- 씬 추가/수정/삭제/순서변경
- 팀원 초대 (가입된 유저를 **이메일**로 추가)
- 프로젝트 탈퇴 (본인, Owner 제외)
- 프로젝트 삭제
```
##### 프로젝트 멤버/권한 (P0)

| 역할 | 권한 |
|---|---|
| **Owner** | 프로젝트 설정 변경, 멤버 초대/삭제, 씬/노드/타임라인 편집, 최종 병합/다운로드 |
| **Editor** | 씬/노드/타임라인 편집, AI 생성 요청, 협업 참여 |
| **Viewer** | 보기 전용(편집 불가), 채팅 참여, 협업 참여(미디어 권한은 Owner가 제한 가능) |

- 기본: 프로젝트 생성자 = **Owner**
- 초대 시 권한( Editor / Viewer )을 선택할 수 있습니다.

##### 프로젝트 상세 화면 탭 구조

| 탭 | 역할 | 주요 기능 |
|---|---|---|
| **Story** | 시나리오/스토리 관리 | AI 시나리오 생성, 씬 목록/순서 관리, 씬 설명 편집 |
| **Scenes** | 영상 진행 현황 | 씬별 확정 영상 현황, 프로젝트 미리보기, 씬 편집 진입점 |
| **Objects** | 오브젝트 시트 관리 | 캐릭터/소품 레퍼런스 이미지 생성 |

**Scenes 탭 상세 (P0):**
```
┌─────────────────────────────────────────────────────────┐
│  📹 프로젝트 미리보기                                     │
│  ┌─────────────────────────────────────────────────┐   │
│  │          [▶ 전체 재생]       12초 / 60초         │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  씬별 영상 현황                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 씬 1: The Discovery     ✓ 3개 영상 (9초)        │   │
│  │ [🎥][🎥][🎥]                      [씬 편집 →]   │   │
│  ├─────────────────────────────────────────────────┤   │
│  │ 씬 2: Entering...       ⏳ 1개 영상 (3초)        │   │
│  │ [🎥]                            [씬 편집 →]    │   │
│  ├─────────────────────────────────────────────────┤   │
│  │ 씬 3: The Revelation    ○ 0개                   │   │
│  │ (아직 없음)                      [씬 편집 →]    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  총 4개 / 12초               [Timeline에서 병합 →]      │
└─────────────────────────────────────────────────────────┘

핵심 기능:
- 프로젝트 상세에서 Depth 없이 영상 진행 상황 파악
- 씬별 확정된 영상 개수/길이 표시
- 전체 미리보기 (순차 재생)
- 씬 편집 화면으로 빠른 진입
- Timeline 화면으로 이동하여 병합
```

#### 4.2.3 오브젝트 시트 생성 (일관성 유지)
```
목적: 영화 전체에서 오브젝트(캐릭터, 소품 등) 외형의 일관성을 유지하기 위한 레퍼런스 이미지 생성

오브젝트 유형:
- 캐릭터: 등장인물 (예: "우주인 민준", "AI 로봇 벨")
- 소품: 주요 물건 (예: "마법 검", "우주선 콘솔")
- 기타: 반복 등장하는 요소

입력:
- 오브젝트 이름
- 유형 (캐릭터/소품/기타)
- 외형 설명 (캐릭터: 나이, 성별, 얼굴 특징, 의상 등 / 소품: 크기, 재질, 색상 등)
- 아트 스타일 (SF 실사, 픽사, 사이버펑크 등)

출력:
- 오브젝트 시트 이미지
  - 캐릭터: 정면/측면/후면 뷰, 표정 예시, 포즈 예시
  - 소품: 다양한 각도, 디테일 뷰

활용 방식:
- 씬 이미지 생성 시 "등장 오브젝트" 선택
- Gemini API 호출 시 오브젝트 시트 이미지를 레퍼런스로 함께 전송
- AI가 레퍼런스를 참고하여 일관된 오브젝트 생성

💡 Gemini 2.5 Pro Image (나노바나나) 모델의 'Identity Preservation' 기능 활용
```

#### 4.2.4 AI 시나리오 생성 (단계별 생성 플로우)
```
═══════════════════════════════════════════════════════════
STEP 1️⃣ 기본 정보 입력
═══════════════════════════════════════════════════════════

입력 항목:
- 장르 (판타지, SF, 로맨스, 액션, 공포, 코미디 등)
- 분위기/톤 (서사적, 코믹, 어두운, 희망적, 감성적 등)
- 원하는 씬 개수 (3~7개)
  - 💡 총 길이(최대 1분) 제한이 있으므로, 씬 개수에 따라 권장 클립 길이를 안내합니다.

선택 입력 (고급 옵션):
- 주제/키워드 (예: "우정", "복수", "희생")
- 메인 캐릭터 힌트 (예: "외로운 우주인", "반항하는 로봇")
- 배경 힌트 (예: "화성 기지", "중세 성")
- 참고할 작품/스타일 (예: "인터스텔라 느낌", "지브리 스타일")

[✨ 시나리오 프롬프트 생성] 클릭

═══════════════════════════════════════════════════════════
STEP 2️⃣ AI 시나리오 프롬프트 검토 및 승인
═══════════════════════════════════════════════════════════

AI가 생성한 시나리오 프롬프트 (편집 가능):
┌─────────────────────────────────────────────────────────┐
│ 🎬 시나리오 방향                                         │
│ ─────────────────────────────────────────────────────── │
│ 화성에 홀로 남겨진 우주인이 고장난 통신 장비를 수리하며  │
│ 지구와의 교신을 시도하는 이야기. 희망과 절망 사이에서    │
│ 인간의 의지를 보여주는 감동적인 SF 단편.                 │
│                                                         │
│ 🎭 핵심 갈등: 고립 vs 연결에 대한 갈망                    │
│ 🎨 톤: 서사적이면서 희망적인                              │
│ ⏱️ 예상 구성: 5개 씬, 각 약 10~12초                       │
│                                  [✏️ 수정] [🔄 재생성]   │
└─────────────────────────────────────────────────────────┘

💡 프롬프트가 마음에 들면 [승인] 버튼을 눌러 다음 단계로 진행
💡 수정이 필요하면 직접 편집하거나 [재생성] 요청

                                              [✅ 승인]

═══════════════════════════════════════════════════════════
STEP 3️⃣ 전체 줄거리 생성
═══════════════════════════════════════════════════════════

승인된 프롬프트를 바탕으로 AI가 전체 줄거리를 생성:
┌─────────────────────────────────────────────────────────┐
│ 📖 전체 줄거리                                           │
│ ─────────────────────────────────────────────────────── │
│ 화성 탐사 기지의 마지막 생존자 민준은 3개월째 홀로       │
│ 버티고 있다. 갑작스러운 모래폭풍으로 동료들을 잃고,      │
│ 지구와의 통신도 끊겼다. 그는 매일 아침 일어나 고장난     │
│ 통신 장비를 수리하며 지구 귀환의 꿈을 놓지 않는다.       │
│ 마침내 통신이 복구되는 순간, 그는 딸의 목소리를 듣게     │
│ 되고, 살아야 할 이유를 되찾는다.                         │
│                                         [✏️ 편집 모드]   │
└─────────────────────────────────────────────────────────┘

💡 줄거리 확인 후 [씬 생성] 버튼으로 씬별 상세 스토리 생성
💡 줄거리 수정 시 씬 재생성 필요

                                         [🎬 씬 생성]

═══════════════════════════════════════════════════════════
STEP 4️⃣ 씬별 스토리 생성 및 편집
═══════════════════════════════════════════════════════════

생성된 씬 목록 (드래그로 순서 변경):
┌─────────────────────────────────────────────────────────┐
│ ≡ 씬 1: 고립된 아침                                      │
│   화성 기지 아침. 창밖으로 붉은 사막이 펼쳐진다.          │
│   민준은 텅 빈 식당에서 혼자 아침을 먹으며...            │
│                                  [✏️ 편집] [🔄 재생성]   │
├─────────────────────────────────────────────────────────┤
│ ≡ 씬 2: 수리의 시작                                      │
│   우주복을 입고 기지 외부로 나선다. 모래폭풍에 손상된     │
│   안테나를 바라보며 결의를 다진다...                     │
│                                  [✏️ 편집] [🔄 재생성]   │
├─────────────────────────────────────────────────────────┤
│ ≡ 씬 3: 희망의 불빛                                      │
│   ...                                                   │
├─────────────────────────────────────────────────────────┤
│ ≡ 씬 4: 교신 성공                                        │
│   ...                                                   │
├─────────────────────────────────────────────────────────┤
│ ≡ 씬 5: 재회의 약속                                      │
│   ...                                                   │
└─────────────────────────────────────────────────────────┘

                              [+ 씬 추가] [🔄 전체 재생성]

편집 기능:
- 드래그 앤 드롭으로 씬 순서 변경
- 각 씬 제목/설명 인라인 편집 가능
- 개별 씬 재생성 또는 전체 재생성
- 씬 추가/삭제

💡 여기서 수정한 씬 설명 = 씬 편집 화면의 씬 설명 (양방향 동기화)
💡 전체 흐름을 한눈에 보며 스토리 구조 조정 가능
```

#### 4.2.5 협업 (WebRTC) - 플로팅 협업 바
```
┌─────────────────────────────────────────────────────────────────────┐
│  어느 화면에서든 "협업 시작" 클릭 → 플로팅 협업 바 활성화             │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  현재 화면 (씬 편집 / 타임라인 / 프로젝트 상세 등)            │   │
│  │                                                             │   │
│  │           (메인 작업 영역)                                  │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 👤👤👤 | 팀원3명 참여 | [🎤] [📷] [🖥️화면공유] [💬채팅] [X]  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│    ↑ 하단 플로팅 협업 바 (접기/확장 가능)                           │
└─────────────────────────────────────────────────────────────────────┘

핵심 개념:
- 협업 바는 별도 화면이 아닌 **오버레이**로 모든 화면 위에 표시
- 프로젝트 상세 → 씬 편집 → 타임라인 이동해도 **통화 유지**
- 화면 공유: 현재 보고 있는 화면을 그대로 공유

기능:
- 화상 통화 시작/종료
- 마이크 ON/OFF
- 카메라 ON/OFF
- 화면 공유 (현재 보고 있는 화면)
- 참여자 목록 (아바타 클릭 시 확장)
- 채팅 (사이드 패널로 확장)
- **Presence 표시** (팀원 현재 작업 위치)

🎯 Presence 표시 (P0):
   - 사이드바/협업 바에서 "SJ → Scene 1 편집 중" 형태로 표시
   - 씬 카드에 현재 작업 중인 팀원 아바타 뱃지
   - WebSocket으로 화면 이동 시 location 이벤트 브로드캐스트
   - 별도 Yjs 없이 구현 가능 (간단한 상태 동기화)

💡 MVP 범위:
   - 어느 화면에서든 협업 시작 가능
   - 화면 이동해도 통화 유지 (Discord 허들처럼)
   - 화면 공유로 같은 화면 보며 토론
   - **Presence 표시로 팀원 작업 위치 파악**
   - (P1) Yjs로 실시간 동시 편집까지 확장
```

#### 4.2.6 스토리보드 생성 (노드 기반 워크플로우)

**노드 타입 5가지:**
| 타입 | 아이콘 | 출력 | 생성 방법 | 프롬프트 플로우 |
|------|-------|------|----------|----------------|
| 씬 헤더 | 📖 | - (정보 표시) | 씬 진입 시 자동 | - (읽기 전용) |
| 마스터 이미지 | 🎬 | 1장 와이드샷 | 씬 헤더 아래 자동 | 입력 → AI 생성 → **검토/수정** → 승인 |
| 스토리보드 그리드 | 📐 | 1장 그리드 이미지 | 마스터의 [+] | 입력 → AI 생성 → **검토/수정** → 승인 |
| 샷 | 📷 | 1장 고품질 이미지 | 그리드의 [+] | 입력 → AI 생성 → **검토/수정** → 승인 |
| 영상 | 🎥 | 영상 클립 | 샷의 [+] | 입력 → AI 생성 → **검토/수정** → 승인 |

> 💡 **모든 생성 노드**에 "입력 → AI 프롬프트 생성 → 검토/수정 → 승인 → 결과 생성" 플로우 적용
> 💡 **씬 헤더 노드**는 씬 제목/설명 표시용 메타 노드로 AI 생성 대상이 아니며, 자동 생성/동기화됩니다. (생성/삭제 불가)

**노드 구조 (마인드맵 스타일):**
```
┌─────────────────────────────────────────────────────────┐
│  📖 씬 헤더 (읽기 전용)                                   │
│  ─────────────────────────────────────────────────────  │
│  제목: "고립된 아침"                                      │
│  스토리: 화성 기지 아침. 창밖으로 붉은 사막이 펼쳐진다...   │
│                                 [프로젝트 상세에서 편집]  │
└─────────────────────────────────┬───────────────────────┘
                                  │
                                  ▼
             ┌─────────────────────────────┐
             │  🎬 마스터 이미지             │  ← 기본 1개 자동 생성(Active)
             │  "고립된 아침" v1  [+]       │  ← 씬 스토리 기반 프롬프트 생성
             └─────────────┬───────────────┘
                           │ [+] 클릭
                     ┌─────┴─────┐         ← 분기 허용 (여러 그리드 실험)
                     ▼           ▼
               ┌───────────┐ ┌───────────┐
               │ 📐 그리드1 │ │ 📐 그리드2 │
               │ 2x3 [+]   │ │ 3x3 [+]   │
               └─────┬─────┘ └───────────┘
                     │ [+] 클릭
                ┌────┴────┐
                ▼         ▼
           ┌───────┐ ┌───────┐
           │📷 샷A │ │📷 샷B │
           │  [+]  │ │  [+]  │
           └───┬───┘ └───┬───┘
               ▼         ▼
           ┌───────┐ ┌─────────┐
           │🎥영상A│ │🎥 영상B  │  ← [P1] 샷 2개 선택 시 트랜지션 영상
           │(샷A→) │ │시작: A   │
           └───────┘ │끝:   B   │
                     └─────────┘
```

---

**🔷 마스터 이미지 노드 (사이드바 UI):**
```
┌─────────────────────────────────────────────────────────┐
│  🎬 마스터 이미지 생성                                   │
├─────────────────────────────────────────────────────────┤
│  📖 씬 정보 (자동 참조):                                 │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 제목: 고립된 아침                                │   │
│  │ 스토리: 화성 기지 아침. 창밖으로 붉은 사막이...    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  🎨 등장 오브젝트: [✓ 민준] [  로봇 벨] [+ 추가]         │
│  🎨 스타일:      [● 실사] [○ 애니] [○ 픽사]            │
│  ☀️ 시간대:      [● 아침] [○ 낮] [○ 저녁] [○ 밤]       │
│  💭 분위기:      [○ 편안] [● 고독] [○ 긴장]            │
│                                                         │
│  [✨ 프롬프트 생성]                                      │
│  ───────────────────────────────────────────────────   │
│  📝 AI 프롬프트 (검토/수정 가능):                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Wide shot of a Mars base cafeteria at dawn.     │   │
│  │ A lone astronaut sits by the window, gazing at  │   │
│  │ the red desert outside. Solitary and reflective │   │
│  │ mood, realistic cinematic style...              │   │
│  └─────────────────────────────────────────────────┘   │
│                         [🔄 재생성]  [✅ 프롬프트 승인]   │
│  ───────────────────────────────────────────────────   │
│  [🎨 이미지 생성]                   상태: ⏳ 승인 대기   │
│                                                         │
│  💡 프롬프트 승인 후 [이미지 생성] 버튼 활성화            │
└─────────────────────────────────────────────────────────┘
```

**마스터 노드 정책 (P0):**
- 씬 진입 시 기본 마스터 1개가 자동 생성되며 `Active Master`로 지정됩니다.
- 자동 생성은 **노드 생성만** 의미하며, 실제 이미지 생성은 사용자가 `[🎨 이미지 생성]`을 눌러 트리거합니다.
- 마스터는 여러 개 생성할 수 있습니다(예: v1, v2, v3). 단, **씬당 최대 3개**로 제한합니다.
- `Active Master`는 1개만 존재하며, 이후 생성되는 그리드/샷/영상은 기본적으로 `Active Master` 아래에 생성됩니다.
- `Active Master` 변경은 “다음 생성의 기준”만 바꾸며, 기존에 만든 그리드/샷/영상은 영향을 받지 않습니다.
- `Active`가 아닌 마스터 브랜치는 기본적으로 접힌 상태로 표시하고(브랜치 접기), 필요 시 펼쳐서 비교합니다.

---

**🔷 스토리보드 그리드 노드 (사이드바 UI):**
```
┌─────────────────────────────────────────────────────────┐
│  📐 스토리보드 그리드 생성                               │
├─────────────────────────────────────────────────────────┤
│  🖼️ 참조 마스터: [썸네일]                                │
│                                                         │
│  📐 레이아웃:  [○ 2x2] [● 2x3] [○ 3x3]                  │
│                                                         │
│  📷 샷 타입 (다중 선택):                                 │
│  [✓ 와이드샷]    [✓ 클로즈업 A]   [✓ 클로즈업 B]       │
│  [  OTS A→B]    [  OTS B→A]     [✓ 소품]              │
│                                                         │
│  💡 구도 힌트 (선택):                                    │
│  [창가 쪽에서 촬영, 붉은 사막이 배경으로 보이게   ]       │
│                                                         │
│  [✨ 프롬프트 생성]                                      │
│  ───────────────────────────────────────────────────   │
│  📝 AI 프롬프트 (검토/수정 가능):                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 2x3 storyboard grid based on Mars base scene.   │   │
│  │ Shots: Wide, Close-up A (face), Close-up B...   │   │
│  └─────────────────────────────────────────────────┘   │
│                         [🔄 재생성]  [✅ 프롬프트 승인]   │
│  ───────────────────────────────────────────────────   │
│  [📐 그리드 생성]                   상태: ⏳ 승인 대기   │
└─────────────────────────────────────────────────────────┘
```

---

**🔷 샷 추출 노드 (사이드바 UI):**
```
┌─────────────────────────────────────────────────────────┐
│  📷 샷 추출 (고품질 재생성)                              │
├─────────────────────────────────────────────────────────┤
│  🖼️ 참조 그리드: [번호 오버레이된 이미지]               │
│                                                         │
│  📍 추출할 샷 번호: [2]                                  │
│     (또는 그리드에서 칸 클릭 → 자동 선택)                │
│                                                         │
│  😊 표정/분위기 조정:                                    │
│  [● 그대로] [○ 밝은 미소] [○ 진지함] [○ 슬픔]          │
│                                                         │
│  💡 추가 디테일 (선택):                                  │
│  [눈에 희망의 빛이 보이도록                    ]         │
│                                                         │
│  [✨ 프롬프트 생성]                                      │
│  ───────────────────────────────────────────────────   │
│  📝 AI 프롬프트 (검토/수정 가능):                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ High quality close-up of astronaut's face.      │   │
│  │ Hopeful expression, light reflecting in eyes... │   │
│  └─────────────────────────────────────────────────┘   │
│                         [🔄 재생성]  [✅ 프롬프트 승인]   │
│  ───────────────────────────────────────────────────   │
│  [📷 샷 생성]                       상태: ⏳ 승인 대기   │
└─────────────────────────────────────────────────────────┘
```

---

**🔷 영상 생성 노드 (사이드바 UI):**
```
┌─────────────────────────────────────────────────────────┐
│  🎥 영상 생성                                            │
├─────────────────────────────────────────────────────────┤
│  📸 시작 샷: [샷 A ✓]                        [변경]     │
│  📸 끝 샷(P1): [+ 샷 선택] (트랜지션용)                  │
│              ※ 클릭 시 샷 노드들 하이라이트              │
│                                                         │
│  🎥 카메라:  [● 줌인] [○ 줌아웃] [○ 패닝] [○ 정적]     │
│  ⏱️ 길이:    [5초 ▾]                                    │
│                                                         │
│  🎭 모션 설명 (선택):                                    │
│  [우주인이 창밖을 바라보다 고개를 돌린다       ]          │
│                                                         │
│  [✨ 프롬프트 생성]                                      │
│  ───────────────────────────────────────────────────   │
│  📝 AI 프롬프트 (검토/수정 가능):                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │ Slow zoom in on astronaut looking out window.   │   │
│  │ Subject turns head slowly. Cinematic, 5 seconds │   │
│  │ Mood: solitary yet hopeful...                   │   │
│  └─────────────────────────────────────────────────┘   │
│                         [🔄 재생성]  [✅ 프롬프트 승인]   │
│  ───────────────────────────────────────────────────   │
│  [🎬 영상 생성]                    상태: ⏳ 승인 대기    │
│  ───────────────────────────────────────────────────   │
│  ★ 완료 후: [타임라인에 확정]                            │
└─────────────────────────────────────────────────────────┘
```

**영상 노드 생성 방법:**
1. **샷 1개 [+]**: 해당 샷이 시작 샷으로 자동 지정
2. **[P1] 샷 2개 선택 → [영상 생성] (트랜지션)**: Ctrl+클릭으로 다중 선택, 선택 순서대로 시작/끝 지정

---

**★ 확정 (Confirm) 개념:**
- 영상 노드에서 "타임라인에 확정" 버튼 클릭
- 확정된 영상만 타임라인 화면에 표시
- 확정 취소 가능, 하나의 샷에서 여러 영상 중 하나만 확정 가능

[P0] Provider: Google Veo 3.1 고정
[P1] 오픈소스 모델 선택 (Stable Video Diffusion 등)

#### 4.2.7 AI 영상 생성 (Image-to-Video)
```
입력:
- 시작 샷 이미지 (필수)
- [P1] 끝 샷 이미지 (선택, 트랜지션 영상용)
- [P0] Provider: Google Veo 3.1 고정
- [P1] Provider 선택: 오픈소스 모델 (Stable Video Diffusion 등)

🎬 카메라 움직임 예시 선택:
- 예시 GIF/이미지와 함께 카메라 움직임 옵션 표시
  • 줌인 → "slow zoom in, cinematic"
  • 줌아웃 → "slow zoom out, revealing shot"
  • 패닝 좌우 → "pan left to right, smooth"
  • 틸트 상하 → "tilt up, dramatic reveal"
  • 정적 → "static camera, subtle movement"
- 선택 시 해당 텍스트가 프롬프트에 자동 추가

🎭 모션 설명 (선택):
- 사용자가 직접 입력하는 동작 묘사
- 예: "두 사람이 대화하며 고개를 끄덕인다", "새들이 날아가는 움직임"

처리:
- 비동기 Job 생성 → 상태 추적 (대기/처리중/완료/실패)
- 진행률 표시: 기본은 상태(대기/진행중/완료/실패)이며, Provider가 세부 단계를 제공하면 진행중 내 세부 단계(Generating/Uploading 등)로 표시
- 백그라운드 생성 지원 (다른 씬 작업 가능)

출력:
- 생성된 영상 미리보기
- 영상 노드에 저장
- 재생성 요청 가능
- "타임라인에 확정" 버튼으로 최종 선택

⚠️ 비용 관리:
- 영상 길이 제한 (씬당 최대 10초, 전체 영화 최대 1분)
- 씬 개수에 따라 "권장 길이"를 자동 안내하여 총 길이를 넘지 않도록 가이드
- 해상도 옵션 (720p/1080p)
- 일일 생성 횟수 제한 (데모용)

💡 MVP 범위 정리:
- P0: Veo 3.1 API, 시작 샷 필수, 카메라 움직임 선택 UI
- P1: 끝 샷 지정 (트랜지션), 오픈소스 모델
```

#### 4.2.8 음악/효과음 추가
```
P1 (외부 업로드/보관):
- 외부에서 생성한 음악 파일 업로드 (MP3, WAV)
- Suno, Udio 등 외부 AI 음악 서비스에서 생성 후 업로드
- 프로젝트별 음악 파일 보관 (메타데이터 저장)
- (선택) 음악 미리듣기

💡 MVP(P0)에서는 배경음악 없이 영상만 병합
💡 P1은 저장/보관까지만, 타임라인 배치/믹싱은 P2

P2 (향후 검토):
- 타임라인 배치/믹싱 및 최종 병합 반영
- AI 음악 생성 API 연동 (API 가용성 확인 필요)
- 현재 Suno 공식 API 미제공으로 보류
```

#### 4.2.9 영상 편집 (2단계 병합 구조)

**📍 STEP 1: 씬 타임라인 (씬 편집 화면 하단)**
```
씬 내 확정된 영상 클립들을 순서대로 배치 → 씬 단위 영상 생성

┌─────────────────────────────────────────────────────────┐
│  씬 1 타임라인 (씬 편집 화면 하단)                        │
│  ┌────────┐ ┌────────┐ ┌────────┐                       │
│  │ 영상A  │ │ 영상B  │ │ 영상C  │  ← 확정된 클립들       │
│  │  3초   │ │  4초   │ │  3초   │                       │
│  └────────┘ └────────┘ └────────┘                       │
│  0s       3s        7s       10s                        │
│                                                         │
│  [▶ 씬 미리보기]         [씬 영상 생성]  상태: ⏳ 대기   │
└─────────────────────────────────────────────────────────┘

기능:
- 클립 순서 변경 (드래그 앤 드롭)
- 클립 확정 해제 (타임라인에서 제거)
- **씬 순차 재생** (▶ 버튼 클릭 시 클립들을 순서대로 자동 연속 재생)
- [씬 영상 생성] → 씬 내 클립들을 병합하여 씬 단위 영상 생성
```

---

**📍 STEP 2: 프로젝트 타임라인 (별도 화면)**
```
각 씬의 병합된 영상들을 모아 최종 영화 생성

┌─────────────────────────────────────────────────────────┐
│  프로젝트 타임라인: "화성 브이로그"                       │
│                                                         │
│  🎬 미리보기                                             │
│  ┌─────────────────────────────────────────────────┐   │
│  │                  [▶ 재생]                        │   │
│  │                 00:25 / 00:45                    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  📹 씬 영상 트랙 (드래그하여 순서 변경)                   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │  씬 1   │ │  씬 2   │ │  씬 3   │ │  씬 4   │       │
│  │ 10초 ✓ │ │ 12초 ✓ │ │ 15초 ✓ │ │ 8초 ⏳ │        │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │
│  0s       10s       22s       37s      45s             │
│                                                         │
│  ⚠️ 씬 4: 씬 영상 미생성 (씬 편집에서 먼저 생성 필요)      │
│                                                         │
│  🎵 음악 파일 보관 (P1)                                  │
│  ┌─────────────────────────────────────────────────┐   │
│  │ ♪ 배경음악.mp3 (저장됨)                          │   │
│  └─────────────────────────────────────────────────┘   │
│  ※ 최종 병합에는 미포함 (P2에서 믹싱)                   │
│                                                         │
│  총 길이: 45초 / 최대 60초                               │
│                                                         │
│  [최종 영상 병합]                        [다운로드 MP4]  │
└─────────────────────────────────────────────────────────┘

기능:
- 씬 순서 변경 (드래그 앤 드롭)
- 씬 영상 미생성 시 경고 표시
- **프로젝트 순차 재생** (▶ 버튼 클릭 시 모든 씬 영상을 순서대로 자동 연속 재생)
- [최종 영상 병합] → 모든 씬 영상을 병합하여 최종 영화 생성
```

---

**📋 병합 단계 요약:**
| 단계 | 위치 | 입력 | 출력 |
|------|------|------|------|
| **씬 병합** | 씬 편집 화면 | 확정된 영상 클립들 | 씬 단위 영상 (예: scene1.mp4) |
| **최종 병합** | 프로젝트 타임라인 | 모든 씬 영상들 | 최종 영화 (예: movie.mp4) |

**MVP (P0):**
- 씬 타임라인: 클립 순서 조정, **순차 재생**, 씬 영상 생성
- 프로젝트 타임라인: 씬 순서 조정, **순차 재생**, 최종 영상 병합
- 출력 규격: 720p/1080p / 24fps / H.264 (audio: 없음)
- 다운로드: MP4

**P1:**
- 분할/트리밍: 영상 시작/끝 지점 조절
- 음악 파일 업로드/보관 (편집/믹싱은 P2)

**P2:**
- 음악 트랙 편집/믹싱: 배경음악을 타임라인에 배치하고 최종 병합에 반영
- 자막 추가: 텍스트 오버레이
- 위치/폰트/색상 설정

**기술:** Server-side FFmpeg (Worker 비동기 Job)

#### 4.2.10 프롬프트 생성/AI 개선 (LLM 기반)
```
목적:
- 사용자가 입력한 "씬/연출 정보"를 바탕으로, 이미지/영상 생성에 적합한 프롬프트를 자동 생성/개선합니다.
- 프롬프트 생성은 "결과(이미지/영상) 생성"과 분리되며, 사용자가 최종 프롬프트를 확인/수정 후 생성 요청을 합니다.

✅ 기본 입력(필수, MVP):
- nodeType (master/grid/shot/video)
- 씬 한줄 설명 (sceneOneLine)
- 스타일(style), 시간대(timeOfDay), 분위기(mood)
- (선택) 등장 오브젝트 IDs + 오브젝트 시트 레퍼런스(일관성 유지)
- (video) 카메라 모션(cameraMotion) + 길이(duration)

▾ 고급 입력(선택, Advanced):
- (video) 모션 설명(motionDescription)
- 렌즈/조명/색감 옵션(lens/lighting/color)
- 금지 요소(negativeKeywords) / 품질 가드레일(워터마크/텍스트 제거 등)
- (P1) 끝 샷 지정(endShot) - 트랜지션 영상용

출력:
- prompt (필수)
- negativePrompt (선택)
- meta (선택): tags, 요약, 버전 정보 등

연동 API:
- `POST /api/ai/prompts/generate`
- `POST /api/ai/prompts/improve`
```

### 4.3 UX 가이드라인

#### 4.3.1 노드 기반 워크플로우 원칙 (Vue Flow 캔버스)
```
┌─────────────────────────────────────────────────────────────────────────┐
│  노드 캔버스 (씬 편집 화면) - 세로 흐름 (위→아래)                          │
│                                                                         │
│                    ┌───────────────────┐                                │
│                    │   🎬 마스터 이미지  │                                │
│                    │   ✓ 완료          │                                │
│                    └─────────┬─────────┘                                │
│                              │                                          │
│               ┌──────────────┼──────────────┐                           │
│               ▼              │              ▼                           │
│        ┌───────────┐         │       ┌───────────┐                      │
│        │ 그리드 v1  │         │       │ 그리드 v2  │  ← 좌우로 분기       │
│        │ 2x3       │         │       │ 3x3       │                      │
│        └─────┬─────┘         │       └─────┬─────┘                      │
│              │               │             │                            │
│         ┌────┴────┐          │             ▼                            │
│         ▼         ▼          │       ┌───────────┐                      │
│   ┌─────────┐ ┌─────────┐    │       │  샷 C     │                      │
│   │ 샷 A    │ │ 샷 B    │    │       │ (대기중)  │                      │
│   └────┬────┘ └────┬────┘    │       └───────────┘                      │
│        ▼           ▼         │                                          │
│  ┌──────────┐ ┌──────────┐   │                                          │
│  │ 영상 A   │ │ 영상 B   │   │                                          │
│  │ ★ 확정  │ │ 생성중   │   │                                          │
│  └──────────┘ └──────────┘   │                                          │
│                              │                                          │
│  💡 세로 흐름: 위→아래로 진행, 분기는 좌우로 펼쳐짐                        │
│  💡 확정(★) 표시: "타임라인에 확정" 클릭한 영상만 최종 편집에 사용        │
└─────────────────────────────────────────────────────────────────────────┘

규칙:
- 마스터 이미지는 여러 개 생성 가능 (씬당 최대 3개)하며, Active Master 1개를 기준으로 작업
- 마스터 이미지에서 여러 그리드 분기 가능 (실험 장려)
- Active가 아닌 마스터 브랜치는 기본 접기(폴딩)로 표시하여 캔버스 복잡도를 제어
- 각 노드는 생성 상태 표시 (대기/생성중/완료/실패)
- 완료된 노드에서 "확정" 버튼 → 타임라인에 확정
- 확정된 영상만 타임라인 화면에 표시
- 모든 분기 히스토리는 DB에 저장 (세션 종료 후에도 유지)

⚠️ 삭제 경고:
"이 노드를 삭제하면 하위 노드들도 함께 삭제됩니다. 계속하시겠습니까?"
```


#### 4.3.2 AI 생성 대기 UX
```
┌─────────────────────────────────────────────────────────┐
│ 📊 Shot 생성 중... (약 20초 남음)                        │
│ ████████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░    │
│                                                         │
│ 💡 이 시간에 할 수 있는 것:                              │
│   • [다음 씬 미리 작성하기]                              │
│   • [다른 샷 카메라 설정하기]                            │
│   • [씬 목록으로 이동]                                  │
└─────────────────────────────────────────────────────────┘

원칙:
- 진행 상태(대기/진행중/완료/실패) + 예상 시간(가능한 경우) 표시
- 대기 시간에 할 수 있는 작업 링크 제공
- 백그라운드 생성 지원으로 다른 씬 작업 가능
```

#### 4.3.3 에러 처리 원칙
```
┌─────────────────────────────────────────────────────────┐
│ ❌ 영상 생성 실패                                        │
│                                                         │
│ 원인: Veo API 일시적 오류                               │
│                                                         │
│ 권장 조치:                                              │
│   • [🔄 다시 시도] - 동일 설정으로 재시도                │
│   • [📝 프롬프트 수정] - 설정 변경 후 재시도             │
│   • [🔙 이전 단계로] - 다른 이미지로 시도                │
│                                                         │
│ 💡 10분 이상 지속되면 다른 시간대에 시도해주세요         │
└─────────────────────────────────────────────────────────┘

원칙:
- 에러 원인 명확히 표시
- 복구 방법 3가지 이상 제안
- 영구적 문제 시 대안 안내
```

#### 4.3.4 피드백 즉시성
```
┌─────────────────────────────────────────────────────────┐
│ 사용할 샷 선택 (드래그로 순서 변경):                     │
│ ┌────────┐ ┌────────┐ ┌────────┐                       │
│ │ [✓] ①  │ │ [✓] ②  │ │ [✓] ③  │  ← 선택 순서 표시    │
│ │와이드샷 │ │민준CU  │ │로봇CU  │                       │
│ └────────┘ └────────┘ └────────┘                       │
│                                                         │
│ 📹 예상 타임라인:                                        │
│ [①와이드 5초]─[②민준CU 5초]─[③로봇CU 5초]  총 15초     │
└─────────────────────────────────────────────────────────┘

원칙:
- 샷 선택 시 예상 타임라인 실시간 프리뷰
- 설정 변경 시 결과 미리보기 제공
- 모든 액션에 즉각적인 시각적 피드백
```

#### 4.3.5 고급 옵션(접기) 원칙
```
┌─────────────────────────────────────────────────────────┐
│ ✅ 기본 입력 (MVP)                                       │
│  - 씬 한줄 설명 (필수)                                   │
│  - 등장 오브젝트 선택 (선택)                             │
│  - 스타일/시간대/분위기 (선택)                           │
│  - (영상) 카메라/길이 (필수)                             │
│                                                         │
│ ▾ 고급 옵션 (기본 접힘, Advanced)                         │
│  - 모션 설명 / 렌즈 / 조명 / 색감 / 금지 요소 (선택)       │
│  - (P1) 끝 샷 지정(트랜지션)                              │
└─────────────────────────────────────────────────────────┘

원칙:
- MVP 기본 입력은 최소화하고, 세부 조정은 고급 옵션으로 접어서 제공
- 고급 옵션 펼침 상태는 사용자 단위로 기억(localStorage 등)하여 반복 입력 부담을 줄임
```

#### 4.3.6 백그라운드 생성 알림 (완료/실패)
```
표시 방식(예시):
- 노드 배지: 대기/생성중/완료/실패
- 토스트 알림(완료): "샷 B 생성 완료"  [열기]
- 토스트 알림(실패): "영상 생성 실패" [재시도] [프롬프트 수정]

원칙:
- AI Provider가 % 진행률을 제공하지 않으면, 상태 기반(대기/진행중/완료/실패)으로 표시
- 완료/실패는 WebSocket 이벤트로 즉시 반영하고, 상세는 필요 시 Job 조회 API로 확인
```

#### 4.3.7 재생성(버전) UX 원칙
```
원칙:
- 재생성(regenerate)은 기존 결과를 덮어쓰지 않고, 동일 부모 아래 "새 버전 노드"를 생성(히스토리 보존)
- 새로 생성된 버전은 UI에서 기본 선택/포커스(최신 버전 자동 선택)
- 사용자는 썸네일/버전 리스트에서 이전 버전으로 언제든 전환 가능
- 확정(Confirm)은 버전 선택과 별개이며, 타임라인에는 "확정된 video"만 반영
```

---

## 5. 사용자 플로우 (User Flow)


### 5.1 전체 플로우

```
┌──────────────────────────────────────────────────────────────────┐
│ 1️⃣ 기획 단계                                                     │
│    • 로그인 → 새 프로젝트 생성                                    │
│    • (선택) 팀원 초대 → 플로팅 협업 바로 화상통화 브레인스토밍          │
│    • AI 시나리오 생성으로 아이디어 구체화                         │
│    • 오브젝트 시트 생성 (주요 오브젝트 레퍼런스 이미지)           │
│    • 씬 구조 설계 (씬 추가 및 설명 작성)                          │
└──────────────────────────────────────────────────────────────────┘
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 2️⃣ 씬 편집 단계 (노드 기반 캔버스)                                │
│                                                                  │
│    🔷 마스터 노드 생성                                            │
│    • 씬 설명 + 등장 오브젝트 선택 → Gemini로 마스터 이미지 생성   │
│    • 마음에 들 때까지 재생성 (마스터가 가장 중요!)                │
│                                                                  │
│    🔷 그리드 노드 생성 (마스터에서 분기 가능)                      │
│    • 샷 타입 선택 → 그리드 이미지 생성                            │
│    • 마음에 안 들면 새 그리드로 분기 (히스토리 보존)              │
│                                                                  │
│    🔷 샷 노드 생성 (그리드에서 분기)                               │
│    • [P0] AI로 고품질 재생성                                      │
│    • [P2] 직접 크롭 (향후 검토)                                   │
│                                                                  │
│    🔷 영상 노드 생성 (샷에서 분기)                                 │
│    • 카메라 움직임 선택 → Veo 3.1로 영상 생성                      │
│    • 마음에 드는 영상에 ★ 확정 → 타임라인에 확정                  │
│                                                                  │
│    💡 분기 가능: 각 단계에서 여러 버전 실험 가능                   │
│    💡 확정 개념: 확정된 영상만 타임라인에 반영                     │
└──────────────────────────────────────────────────────────────────┘
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 3️⃣ 음악 파일 업로드/보관 (P1)                                    │
│    • 외부에서 생성한 배경음악 파일 업로드                         │
│    • (Suno, Udio 등에서 생성 후 업로드)                           │
│    • 타임라인 편집/믹싱은 P2                                      │
└──────────────────────────────────────────────────────────────────┘
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 4️⃣ 편집/완성 단계                                                │
│    • 타임라인에서 ★ 확정된 영상 클립들 확인                       │
│    • 순서 조정 (드래그 앤 드롭)                                   │
│    • (P1) 트리밍으로 구간 조절                                    │
│    • "영상 병합" 클릭 → 최종 영상 생성                            │
│    • 미리보기 → 다운로드                                          │
└──────────────────────────────────────────────────────────────────┘
```

---

## 6. 화면 구성

### 6.1 주요 화면 목록

| 화면 | 설명 | 플랫폼 |
|------|------|--------|
| 랜딩 페이지 | 서비스 소개, 로그인/회원가입 CTA | Web |
| 로그인/회원가입 | 인증 화면 | Web |
| 대시보드 | 프로젝트 목록, 새 프로젝트 생성 | Web |
| 프로젝트 상세 | 시나리오, 씬 목록, **협업 시작 버튼** | Web |
| **플로팅 협업 바** | **모든 화면 하단에 오버레이** - 화상통화+화면공유 (화면 이동해도 유지) | Web |
| 씬 편집 | **노드 기반 캔버스 (Vue Flow)**: 마스터→그리드→샷→영상 분기/확정 | Web |
| 타임라인/편집 | **확정된 영상만** 표시, 순서 조정, 병합 | Web |
| 미리보기 | 최종 영상 재생, 다운로드 | Web |

### 6.2 화면별 상세 레이아웃

#### 6.2.1 대시보드
```
┌─────────────────────────────────────────────────────────────────┐
│  🎬 AI Movie Studio                    [프로필] [로그아웃]      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  내 프로젝트                              [+ 새 프로젝트]       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ 🎬          │  │ 🎬          │  │ 🎬          │             │
│  │ 썸네일       │  │ 썸네일      │  │ 썸네일      │             │
│  │             │  │             │  │             │             │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤             │
│  │ 용사의 모험 │  │ SF 단편     │  │ 로맨스 MV   │             │
│  │ 씬 5개      │  │ 씬 3개      │  │ 씬 4개      │             │
│  │ 2026-01-12  │  │ 2026-01-10  │  │ 2026-01-08  │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**프로젝트 카드 썸네일 규칙 (MVP):**
- 기본 썸네일 = **첫 번째 씬(Scene 1)**의 `Active Master` 이미지
- 아직 `Active Master` 이미지가 없으면 기본 플레이스홀더를 표시
- (선택, P1) 프로젝트 설정에서 커버(썸네일) 수동 지정 지원

#### 6.2.2 프로젝트 상세 (스토리 구성 + 씬 관리)
```
┌─────────────────────────────────────────────────────────────────┐
│  ← 대시보드    "화성 브이로그"            [협업 시작] [설정]      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ═══════════════════════════════════════════════════════════   │
│  📖 스토리 구성                                                 │
│ ═══════════════════════════════════════════════════════════   │
│                                                                 │
│  ✨ 스토리/연출 프롬프트                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 장르: [SF▼]  분위기: [브이로그▼]  씬 개수: [5개▼]        │   │
│  │                                                         │   │
│  │ 전체 줄거리:                                            │   │
│  │ 화성 기지에서 하루를 보내는 우주인의 브이로그.          │   │
│  │ 아침 루틴부터 탐사, 식사까지 화성 생활을 담은 콘텐츠.   │   │
│  │                                         [AI로 씬 생성]  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  🎬 씬별 스토리 (드래그로 순서 변경)                [+ 씬 추가] │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ≡ 1│ 화성 기지 아침. 창밖 붉은 사막...     [✏️ 편집]   │ ↕ │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ ≡ 2│ 우주복 입고 탐사 준비...              [✏️ 편집]   │ ↕ │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ ≡ 3│ 화성 표면 첫 발걸음. 카메라 손 흔들기 [✏️ 편집]   │ ↕ │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ ≡ 4│ 로버 타고 협곡 탐험...               [✏️ 편집]   │ ↕ │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ ≡ 5│ 기지 복귀, 저녁 식사 & 마무리...     [✏️ 편집]   │ ↕ │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  💡 [✏️ 편집] 클릭 → 해당 씬 편집 화면으로 이동                  │
│  💡 씬 설명 수정 시 씬 편집 화면과 자동 동기화                   │
│                                                                 │
│ ═══════════════════════════════════════════════════════════   │
│  🎨 오브젝트 시트                            [오브젝트 관리]    │
│ ═══════════════════════════════════════════════════════════   │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐                  │
│  │ 우주인 민준│ │ AI 로봇 벨│ │ [+ 추가]   │                  │
│  └────────────┘ └────────────┘ └────────────┘                  │
│                                                                 │
│  [타임라인으로 이동 →]                                          │
│                                                                 │
│  💡 배경음악은 업로드/보관(P1)만 지원하며, 믹싱은 P2에서 검토합니다. │
│  💡 협업은 상단 [협업 시작] 버튼으로 언제든 시작할 수 있습니다.  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 6.2.3 씬 편집 화면 (노드 기반 캔버스 + Vue Flow)
```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  ← 프로젝트    씬 1: "화성 기지 아침"       [협업 시작] [타임라인으로] [패널 토글 ☰]   │
├───────────────────────────────────────────────────────────────────┬─────────────────┤
│                                                                   │ 📋 노드 상세    │
│  🎨 노드 캔버스 (메인 영역)                    [줌+] [−] [미니맵]  │                 │
│ ┌───────────────────────────────────────────────────────────────┐ │ 노드: 샷 A     │
│ │                                                               │ │ 타입: 샷       │
│ │              ┌─────────────────────┐                          │ │ 상태: ✓ 완료   │
│ │              │  🎬 마스터 이미지    │                          │ │ ─────────────  │
│ │              │  ✓ 완료 [+분기]     │                          │ │ 프롬프트:      │
│ │              └──────────┬──────────┘                          │ │ ┌───────────┐ │
│ │                         │                                     │ │ │ 창가에    │ │
│ │            ┌────────────┴────────────┐                        │ │ │ 서있는..  │ │
│ │            ▼                         ▼                        │ │ └───────────┘ │
│ │     ┌────────────┐            ┌────────────┐                  │ │ [AI 개선 ✨]  │
│ │     │ 그리드 v1  │            │ 그리드 v2  │                  │ │               │
│ │     │ 2x3 ✓     │            │ 3x3 ✓     │                  │ │ 카메라: [줌인] │
│ │     └─────┬──────┘            └─────┬──────┘                  │ │ 길이: [5초]   │
│ │           │                         │                         │ │               │
│ │      ┌────┴────┐                    ▼                         │ │ [🔄 재생성]   │
│ │      ▼         ▼              ┌───────────┐                   │ │ [▶ 영상생성]  │
│ │ ┌─────────┐ ┌─────────┐       │ 샷 C      │                   │ │ [★ 확정]     │
│ │ │ 샷 A ✓  │ │ 샷 B    │       │ 생성중    │ ← 상태를 노드에   │ │               │
│ │ └────┬────┘ │ 생성중  │       └───────────┘   직접 표시        │ │               │
│ │      ▼      └─────────┘                                       │ │               │
│ │ ┌──────────┐                                                  │ │               │
│ │ │ 영상 A   │                                                  │ │               │
│ │ │ ★ 확정  │                                                  │ │               │
│ │ └──────────┘                                                  │ │               │
│ │                                                               │ │               │
│ └───────────────────────────────────────────────────────────────┘ │               │
│  💡 클릭: 상세  |  우클릭: 메뉴  |  드래그: 분기                    │ [패널 접기 →] │
├───────────────────────────────────────────────────────────────────┴─────────────────┤
│  ★ 확정된 영상 미니 타임라인                                    [타임라인 화면 →]   │
│  ┌──────────┐                                                                      │
│  │ 영상A 5초 │  ──────▶  (확정 추가 시 여기 표시)                총 5초             │
│  └──────────┘                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘

레이아웃 원칙:
- 노드 캔버스가 화면의 70% 이상 차지 (메인 포커스)
- 노드 상세 패널: 오른쪽 사이드바 (접기 가능)
- 진행 상황: 각 노드에 직접 표시 (별도 패널 X)
- 확정 영상: 하단 미니 타임라인 (필요시 접기)
- 마스터는 여러 개 생성 가능하지만 Active Master 1개를 기준으로 작업하며, Active가 아닌 마스터 브랜치는 기본 접기

💡 P1 실시간 협업 (Yjs/CRDT):
- 팀원 커서가 캔버스에 실시간 표시
- 노드 편집 중인 팀원 아바타 표시
```


#### 6.2.4 플로팅 협업 바 (모든 화면 공통 컴포넌트)
```
협업 바는 별도 화면이 아닌, 모든 화면 하단에 표시되는 오버레이:

┌─────────────────────────────────────────────────────────────────┐
│  씬 편집 / 타임라인 / 프로젝트 상세 (어떤 화면이든)            │
│                                                                 │
│                      (메인 작업 영역)                          │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  협업 바 (접기 상태)                       [▲ 확장] [통화 종료 🔴]  │
│  👤👤👤 팀원3명 │ [🎤] [📷] [🖥️화면공유] [💬채팅]                    │
└─────────────────────────────────────────────────────────────────┘

[▲ 확장] 클릭 시:
┌─────────────────────────────────────────────────────────────────┐
│  협업 바 (확장 상태)                                    [▼ 접기]   │
├─────────────────────────────────┬───────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐  │  💬 채팅                      │
│  │  👤 나   │ │ 👤 팀원A │ │ 👤 팀원B │  │  ────────────────────   │
│  │ (호스트) │ │         │ │         │  │  나: 2번 씬 어떻게 생각해?  │
│  └─────────┘ └─────────┘ └─────────┘  │  팀원A: 클로즈업으로 가자    │
│  [🎤 ON] [📷 ON] [🖥️ 화면공유]        │  ────────────────────   │
│                                   │  [메시지 입력...]        [📤]│
└─────────────────────────────────┴───────────────────────────────┘

핵심:
- 화면 이동해도 통화 유지 (Discord 허들처럼)
- 화면 공유: 현재 보고 있는 화면 그대로 공유
- 접기/확장 토글로 작업 방해 최소화
```

#### 6.2.5 타임라인/편집 화면
```
┌─────────────────────────────────────────────────────────────────┐
│  ← 프로젝트    타임라인 편집                        [미리보기]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🎬 미리보기                                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │                    [영상 미리보기]                      │   │
│  │                         ▶                               │   │
│  │                                                         │   │
│  │                      00:15 / 01:00                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  📹 비디오 트랙 (확정된 클립, 드래그하여 순서 변경)              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ |씬1: 마을 |씬2: 용사 |씬3: 어둠 |씬4: 출발 |           │   │
│  │ | 10초    | 15초     | 20초     | 15초     |           │   │
│  │ |▓▓▓▓▓▓▓▓|▓▓▓▓▓▓▓▓▓▓|▓▓▓▓▓▓▓▓▓▓▓|▓▓▓▓▓▓▓▓▓|            │   │
│  └─────────────────────────────────────────────────────────┘   │
│  0s        15s        30s        50s        60s                 │
│                                                                 │
│  🎵 오디오 트랙 (P2)                                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ |♪ epic_adventure.mp3                                   │   │
│  │ |░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  총 길이: 60초 (1분)                                            │
│                                                                 │
│  [영상 병합하기]                              [다운로드 (MP4)]  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

> 💡 MVP(P0)에서는 배경음악 없이 영상만 병합합니다. P1은 업로드/보관까지만 제공하며, 오디오 트랙 편집/믹싱 UI는 P2에서 활성화합니다.

#### 6.2.6 새 프로젝트 생성 모달
```
┌─────────────────────────────────────────────────────────────────┐
│                     새 프로젝트 만들기                     [X]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  프로젝트 제목 *                                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 용사의 모험                                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  설명                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 젊은 용사가 마을을 구하기 위해 모험을 떠나는 이야기     │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  장르                                                           │
│  [판타지▼]                                                      │
│                                                                 │
│  ✨ AI로 시나리오 아이디어 받기                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 키워드: 판타지, 용사, 마을, 모험, 반전                  │   │
│  │                                    [아이디어 생성 ✨]   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│                              [취소]  [프로젝트 생성]            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. 기술 요구사항

### 7.1 프론트엔드
```
- Vue 3 + Composition API
- Vue Router (SPA 라우팅)
- Pinia (상태 관리)
- Axios (API 통신)
- Vue Flow (노드 기반 씬 편집 캔버스)
- Yjs / y-websocket (P1, 실시간 협업 동기화)
- WebRTC API (화상통화)
- 반응형 디자인 (Desktop 우선)
```

### 7.2 백엔드
```
- Spring Boot 3.x
- Spring Security (JWT 인증, [P1] OAuth2)
- Spring MyBatis
- WebSocket (WebRTC 시그널링, 채팅, Job 완료/실패 알림)
- AWS S3 SDK (파일 업로드)
- Redis Streams (비동기 작업 큐)
- FFmpeg (Server-side 병합/인코딩, Worker)
- Swagger / OpenAPI (API 문서화)
- AI API 연동
  • Google Gemini API (이미지 생성)
  • Google Veo 3.1 API (영상 생성)
  • [P1] 오픈소스 모델 (Stable Video Diffusion 등, 셀프호스팅)
  • 음악: 외부 파일 업로드 (AI 음악 API는 향후 검토)
```

### 7.3 아키텍처

```
[Client: Web Browser]
  ├─ WebRTC P2P Media (화상통화)
  └─ HTTPS → Nginx → Spring API
                         ├─ DB (MySQL)
                         ├─ Redis (Streams + Cache)
                         └─ S3/MinIO (파일 저장)

[Worker]
  └─ Redis Streams 소비 → AI API 호출/FFmpeg 병합 → S3 저장 → DB 업데이트

[DevOps / Infra]
  ├─ Jenkins (CI/CD)
  ├─ Docker Hub / AWS ECR (컨테이너 레지스트리)
  ├─ AWS EC2 / Docker (배포)
  └─ Prometheus + Grafana (모니터링)
```
**WebRTC 구현 메모 (P0):**
- **Room 단위**: 기본은 `projectId` = `roomId` (프로젝트 참여자끼리 동일 룸). 필요 시 `sceneId` 단위 룸으로 확장.
- **시그널링**: WebSocket 기반 시그널링 서버 사용 (8.9 참고).
- **NAT/TURN 대응**: STUN 기본, 데모/초기 운영은 공개 또는 저비용 TURN을 사용하고, P1에서 OpenVidu/Janus 등으로 고도화 검토.


### 7.4 AI API 처리 방식

```
사용자 요청
    ↓
API 서버 (요청 접수 → Redis Streams)
    ↓
Worker (Streams 소비 → AI API 호출 → 결과 저장)
    ↓
상태 업데이트 (DB 업데이트)
    ↓
Redis 이벤트(Pub/Sub 또는 Streams) 발행
    ↓
API 서버(WebSocket 브릿지) → 프로젝트 이벤트 WebSocket으로 완료/실패 알림
```

**AI Job 상태:**
| status (API) | 의미 |
|---|---|
| PENDING | 대기열에서 대기 중 |
| RUNNING | AI 처리 중 |
| SUCCEEDED | 완료 |
| FAILED | 실패 |

> 참고: AI Provider가 % 진행률을 제공하지 않는 경우, UI는 status 기반(`⏳/🔄/✅/❌`)으로 표시하고 완료/실패는 WebSocket 이벤트로 즉시 반영합니다.

### 7.5 노드 기반 워크플로우 DB 스키마

노드 기반 씬 편집을 위해 다음 테이블 구조가 필요합니다:
- **scene_header 노드**는 씬 메타(제목/설명) 표시용 **가상 노드**로, `scenes` 테이블 기반으로 API 응답 시 합성되며 `scene_nodes`에 저장하지 않습니다.

```sql
-- 씬 내 노드 (마스터/그리드/샷/영상)
CREATE TABLE scene_nodes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    scene_id BIGINT NOT NULL,
    node_type ENUM('master', 'grid', 'shot', 'video') NOT NULL,
    parent_node_id BIGINT NULL,          -- 분기 구조 표현 (NULL이면 루트)
    status ENUM('PENDING', 'RUNNING', 'SUCCEEDED', 'FAILED') DEFAULT 'PENDING', -- (API 노드 status와 동일)
    is_active BOOLEAN DEFAULT FALSE,     -- master 전용: Active Master 여부 (scene당 최대 1개)
    is_confirmed BOOLEAN DEFAULT FALSE,  -- 타임라인 확정 여부 (video만)
    content_url VARCHAR(500),            -- S3 이미지/영상 URL
    prompt TEXT,                         -- AI 생성 프롬프트
    settings JSON,                       -- 카메라, 길이, 스타일 등 설정
    
    -- 영상 노드 전용 (video 타입만 사용)
    start_shot_node_id BIGINT NULL,      -- 시작 샷 노드 ID
    end_shot_node_id BIGINT NULL,        -- 끝 샷 노드 ID (선택, 트랜지션용)
    
    position_x FLOAT,                    -- 캔버스 내 X 좌표
    position_y FLOAT,                    -- 캔버스 내 Y 좌표
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    FOREIGN KEY (scene_id) REFERENCES scenes(id) ON DELETE CASCADE,
    FOREIGN KEY (parent_node_id) REFERENCES scene_nodes(id) ON DELETE CASCADE,
    FOREIGN KEY (start_shot_node_id) REFERENCES scene_nodes(id) ON DELETE SET NULL,
    FOREIGN KEY (end_shot_node_id) REFERENCES scene_nodes(id) ON DELETE SET NULL
);

-- 노드 간 연결 (Vue Flow edge)
CREATE TABLE node_edges (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    source_node_id BIGINT NOT NULL,
    target_node_id BIGINT NOT NULL,
    edge_type VARCHAR(50) DEFAULT 'default',
    
    FOREIGN KEY (source_node_id) REFERENCES scene_nodes(id) ON DELETE CASCADE,
    FOREIGN KEY (target_node_id) REFERENCES scene_nodes(id) ON DELETE CASCADE
);
```

**핵심 포인트:**
- `parent_node_id`로 트리 구조 표현 (분기 가능)
- `is_active`로 현재 작업 기준이 되는 Active Master 표현 (master만 사용, scene당 1개)
- `is_confirmed`로 타임라인에 확정된 영상 필터링
- `start_shot_node_id`, `end_shot_node_id`로 영상 노드의 입력 샷 참조
- `position_x/y`로 캔버스 내 노드 위치 저장
- `node_edges`는 (선택) Vue Flow에서 수동 연결 편집을 지원할 때 사용 (P0에서는 트리로부터 계산 가능)

**제약/동시성 보장 (권장):**
- **Active Master 유일성**: `scene_id` 당 `is_active=true` master는 최대 1개 (트랜잭션 + DB 제약으로 보장 권장)
- **확정(Confirm) 유일성**: `start_shot_node_id`(=샷) 당 `is_confirmed=true` video는 최대 1개
  - 새 video를 확정하면, 동일 샷의 기존 확정 video는 원자적으로 해제
- **참조 무결성**: `start_shot_node_id`/`end_shot_node_id`는 `node_type='shot'`만 참조하도록 서버에서 검증

### 7.6 노드 타입별 상세 스펙

| 노드 타입 | 필수 속성 | 가능한 상태 | 연결 가능 대상 |
|-----------|-----------|-------------|----------------|
| **scene_header** | title, description (scene 메타) | — (읽기 전용) | — |
| **master** | prompt, object_ids, style, time_of_day, mood, is_active | PENDING/RUNNING/SUCCEEDED/FAILED | → grid |
| **grid** | layout (2x2, 2x3 등), shot_types[] | PENDING/RUNNING/SUCCEEDED/FAILED | → shot |
| **shot** | shot_number, expression_option | PENDING/RUNNING/SUCCEEDED/FAILED | → video |
| **video** | start_shot_node_id, end_shot_node_id(선택), camera_motion, duration, motion_description | PENDING/RUNNING/SUCCEEDED/FAILED, **is_confirmed** | (종단) |

추가 규칙:
- `video.parent_node_id`는 **시작 샷(start_shot)** 기준으로 연결(트리 구조 유지)
- `end_shot_node_id`는 **선택 참조(크로스 링크)**로 사용하며, 트랜지션 영상(P1)에서만 사용
- 재생성(regenerate)으로 생성된 버전 노드들은 동일 부모 아래 sibling으로 누적되어 히스토리를 구성

**노드 연결 규칙 (세로 흐름):**
```
        ┌───────────────┐
        │    master     │ ← 씬당 여러 개 (루트, Active 1개)
        └───────┬───────┘
                │
       ┌────────┴────────┐
       ▼                 ▼
 ┌───────────┐     ┌───────────┐
 │  grid v1  │     │  grid v2  │  ← 좌우 분기 가능
 └─────┬─────┘     └─────┬─────┘
       │                 │
  ┌────┴────┐            ▼
  ▼         ▼      ┌───────────┐
┌─────┐ ┌─────┐    │  shot 3   │
│shot1│ │shot2│    └─────┬─────┘
└──┬──┘ └──┬──┘          │
   ▼       ▼             ▼
┌─────┐ ┌─────┐    ┌───────────┐
│videoA│ │videoB│  │  video C  │
│ ★확정│ │      │  │           │
└─────┘ └─────┘    └───────────┘

규칙:
- master는 씬당 여러 개 생성 가능 (루트 노드, Active 1개)
- master → grid 만 연결 가능
- grid → shot 만 연결 가능
- shot → video 만 연결 가능
- 동일 부모에서 여러 자식 분기 가능 (실험 허용)
- video 노드만 "확정" 가능
```

### 7.7 타임라인-노드 동기화 흐름

```
[씬 편집 화면]                              [타임라인 화면]
      │                                           │
      │   ★ "타임라인에 확정" 클릭                  │
      ├──────────────────────────────────────────▶│
      │   - is_confirmed = true                   │
      │   - 해당 씬의 확정된 영상 목록 갱신           │
      │                                           │
      │   ★ 확정 취소 클릭                          │
      ├──────────────────────────────────────────▶│
      │   - is_confirmed = false                  │
      │   - 타임라인에서 제거                       │
      │                                           │

동기화 규칙:
- 하나의 shot에서 여러 video 중 최대 1개만 확정 가능
- 씬별로 여러 shot의 video가 각각 확정될 수 있음
- 타임라인 순서는 확정된 영상들의 생성 순서 또는 사용자 드래그로 결정
- 확정 상태 변경 시 실시간으로 타임라인 UI 업데이트
```

### 7.8 P1 실시간 협업 (Yjs/CRDT) 상세 스펙

**동기화 대상:**
```
┌───────────────────────────────────────────────────────────┐
│ Yjs Document 구조                                         │
├───────────────────────────────────────────────────────────┤
│ - Y.Map: 노드 목록 (id → 노드 데이터)                      │
│ - Y.Map: 엣지 목록 (id → 연결 데이터)                      │
│ - Y.Map: 노드 위치 (id → {x, y})                          │
│ - Y.Map: 사용자 커서 위치 (awareness)                      │
│ - Y.Array: 확정된 영상 순서                                 │
└───────────────────────────────────────────────────────────┘
```

**실시간 표시 항목:**
- 각 팀원의 커서 위치 (아바타 + 이름)
- 현재 편집 중인 노드 하이라이트 (잠금은 아님, 표시만)
- 노드 생성/삭제/연결 실시간 반영
- 확정 상태 변경 실시간 반영

**충돌 해결:**
- CRDT 기반 자동 병합 (Last-Writer-Wins가 아닌 구조적 병합)
- 동시에 같은 프롬프트 수정 시: 두 내용 모두 유지 후 사용자 선택

---

## 8. API 명세 (초안)

### 8.0 공통 규칙

- **인증**: `Authorization: Bearer <JWT>`
- **토큰 정책**: Access Token 1일, Refresh Token 7일 (Redis 저장 + Rotation)
- **페이지네이션(목록 API)**: `?page=0&size=20` (기본 0/20)
- **삭제 정책(MVP)**: **하드 delete** (soft delete 미사용)
- **공통 응답 포맷**: 모든 JSON REST API는 공통 래퍼를 사용합니다.
  - `code`는 도메인 문자열 코드이며 HTTP 상태 코드는 헤더로만 전달합니다.  
  - 성공 code 값: `SUCCESS`(즉시 성공), `ACCEPTED`(비동기 작업 접수)
  - 실패 시 상세 정보는 `details`에 담습니다.

```json
{
  "code": "SUCCESS",
  "message": "요청이 성공적으로 처리되었습니다.",
  "data": { ... }
}
```

```json
{
  "code": "FORBIDDEN",
  "message": "You do not have permission to edit this project.",
  "details": {
    "projectId": 123
  }
}
```

- **비동기 AI Job 응답 규칙(예시)**
  - 생성 요청은 `202 Accepted` + `jobId` 반환
  - 클라이언트는 **프로젝트 이벤트 WebSocket(8.10)** 으로 완료/실패 이벤트를 받고, 필요 시 `GET /api/ai/jobs/{jobId}`로 결과/에러를 조회
  - (Fallback) WebSocket 연결이 없으면 `GET /api/ai/jobs/{jobId}` 폴링으로 상태를 확인
  - `progress`(%)는 AI Provider에 따라 **미지원일 수 있으며**, MVP에서는 status 기반 UI를 기본으로 함

```json
{
  "code": "SUCCESS",
  "data": {
    "jobId": 12345,
    "type": "VIDEO_GENERATION",
    "status": "RUNNING",
    "progress": null,
    "target": { "type": "NODE", "id": 987 },
    "resultUrl": null,
    "error": null
  }
}
```

- **중복 요청 방지(선택)**: `Idempotency-Key` 헤더를 지원하여 동일 키로 재요청 시 동일 `jobId`를 반환

### 8.1 인증 API
```
POST /api/auth/signup       - 회원가입
POST /api/auth/login        - 로그인
POST /api/auth/refresh      - 토큰 재발급 (Rotation)
POST /api/auth/logout       - 로그아웃
GET  /api/auth/me           - 내 정보 조회
PUT  /api/auth/me           - 내 프로필 수정 (이름/프로필 이미지 등)

💡 토큰 응답 규칙(MVP):
  - login/refresh 응답: { code: "SUCCESS", data: { accessToken, refreshToken, expiresIn } }
  - logout: Refresh Token 무효화 (Redis 삭제)

[P0] 비밀번호 재설정 (이메일)
POST /api/auth/password/reset/request - 재설정 요청 (이메일로 링크/코드 발송)
POST /api/auth/password/reset/confirm - 재설정 완료 (token/code + 새 비밀번호)

[P1] OAuth 소셜 로그인
POST /api/auth/oauth/google - Google OAuth 로그인
```

### 8.2 프로젝트 API
```
POST   /api/projects              - 프로젝트 생성
GET    /api/projects              - 프로젝트 목록
GET    /api/projects/{id}         - 프로젝트 상세
PUT    /api/projects/{id}         - 프로젝트 수정
DELETE /api/projects/{id}         - 프로젝트 삭제

GET    /api/projects/{id}/members                         - 멤버 목록
POST   /api/projects/{id}/members                         - 멤버 추가 (Owner만, 가입된 유저만)
  - body: { email: "user@example.com", role: "EDITOR|VIEWER" }
PATCH  /api/projects/{id}/members/{memberId}              - 멤버 권한 변경 (Owner만)
DELETE /api/projects/{id}/members/{memberId}              - 멤버 강퇴 (Owner만)
DELETE /api/projects/{id}/members/me                      - 프로젝트 탈퇴 (본인)

💡 Owner 탈퇴 정책 (MVP):
  - Owner는 `/members/me`로 탈퇴할 수 없습니다. (권한 위임 또는 프로젝트 삭제 필요)
  - (선택, P1) Owner 권한 위임 API로 확장 가능

💡 MVP(P0): 초대 링크/토큰 방식 없이, 가입된 유저만 멤버로 추가합니다.
  - 유저가 없으면 `USER_NOT_FOUND`로 응답하고 "가입 후 다시 초대"를 안내합니다.
💡 (선택, P1): 초대함/알림 기반 '수락' 플로우로 확장 가능
```

### 8.3 오브젝트 시트 API
```
POST   /api/projects/{id}/objects     - 오브젝트 생성 (AI 이미지 생성 Job)
GET    /api/projects/{id}/objects     - 오브젝트 목록
GET    /api/objects/{id}              - 오브젝트 상세
PUT    /api/objects/{id}              - 오브젝트 수정
DELETE /api/objects/{id}              - 오브젝트 삭제
```

### 8.4 씬 API
```
POST   /api/projects/{id}/scenes       - 씬 생성
GET    /api/projects/{id}/scenes       - 씬 목록
GET    /api/scenes/{id}                - 씬 상세 (설명/등장 오브젝트/상태)
PUT    /api/scenes/{id}                - 씬 수정
DELETE /api/scenes/{id}                - 씬 삭제
PUT    /api/projects/{id}/scenes/order - 씬 순서 변경
```

### 8.5 노드 API (씬 편집)
```
POST   /api/scenes/{sceneId}/nodes     - 노드 생성 (마스터/그리드/샷/영상)
GET    /api/scenes/{sceneId}/nodes     - 씬의 노드 트리 조회
GET    /api/nodes/{id}                 - 노드 상세
GET    /api/nodes/{id}/export          - 노드 결과 다운로드 URL 발급 (이미지/영상)
PUT    /api/nodes/{id}                 - 노드 수정 (프롬프트, 설정 등)
PUT    /api/scenes/{sceneId}/nodes/positions - 노드 위치 일괄 저장 (드래그 후)
DELETE /api/nodes/{id}                 - 노드 삭제 (하위 노드 함께)
POST   /api/nodes/{id}/regenerate      - 재생성: 새 버전 노드 생성 + AI Job
POST   /api/nodes/{id}/activate        - Active Master 변경 (master 노드만)
POST   /api/nodes/{id}/confirm         - 타임라인 확정 (영상 노드만)
DELETE /api/nodes/{id}/confirm         - 확정 취소

💡 scene_header 노드:
  - 씬 메타(제목/설명) 표시용 가상 노드로 `GET /api/scenes/{sceneId}/nodes` 응답에 포함됩니다.
  - 생성/삭제 대상이 아니며, 씬 정보와 자동 동기화됩니다.

💡 P0 연결 규칙: 노드 연결은 `parent_node_id` 기반 트리 구조로만 생성(마스터→그리드→샷→영상)
   - Vue Flow용 edge는 서버가 트리로부터 계산해 내려줌(수동 edge 편집은 P1)
💡 Active Master: master는 여러 개 생성 가능하지만 scene당 Active는 최대 1개이며, 신규 그리드는 기본적으로 Active 아래에 생성됩니다.
💡 하드 리밋: 씬당 master 노드 최대 3개로 제한합니다.
💡 확정 규칙: 한 shot 아래 video는 최대 1개만 확정(서버가 자동으로 기존 확정을 해제)
💡 재생성(regenerate) 정책(MVP):
  - 기존 노드를 덮어쓰지 않고, 동일 부모 아래 **새 버전 노드**를 생성합니다(히스토리 보존).
  - 응답 예: `{ jobId, newNodeId }`
  - 새 버전은 UI에서 기본 선택/포커스(최신 버전 자동 선택)됩니다.
  - 실패 재시도 버튼은 `regenerate` 호출을 기본으로 합니다. (같은 jobId 재실행은 운영/관리 목적)
💡 삭제 정책(MVP):
  - 노드 삭제 시 하위 노드가 함께 삭제됩니다.
  - 삭제된 노드에 대해 뒤늦게 도착한 Job 결과는 서버가 무시(무효화)합니다.
💡 다운로드(export) 정책(MVP):
  - `status=SUCCEEDED`인 노드만 다운로드 URL을 발급합니다.
  - response(예): `{ downloadUrl }` (S3 Presigned URL)
```

### 8.6 AI 생성 API
```
POST /api/ai/scenario
  - body: { genre, mood, plot, sceneCount, keywords? }
  - response: { scenario, scenes[] }

💡 프롬프트 생성/개선 (LLM 기반)
POST /api/ai/prompts/generate
  - 목적: UI 입력(씬 한줄 설명/스타일/시간대/분위기/등장 오브젝트 등) → 최종 프롬프트 생성
  - 필수(예): { nodeType, sceneOneLine, style, timeOfDay, mood }
  - 선택(예): { objects[], motionDescription, lens, lighting, color, negativeKeywords[] }
  - response: { prompt, negativePrompt?, meta? }

POST /api/ai/prompts/improve
  - 목적: 기존 프롬프트 + 개선 지시(예: "더 시네마틱하게") → 개선 프롬프트 생성
  - body(예): { nodeType, prompt, instruction?, context? }
  - response: { prompt, negativePrompt?, meta? }

💡 이미지/영상 생성은 노드 중심으로 처리:
POST /api/nodes/{id}/regenerate
  - node_type에 따라 작업이 결정됨 (master/grid/shot → IMAGE, video → VIDEO)
  - video 노드 settings 예: { startShotNodeId, endShotNodeId?, cameraMotion, duration, motionDescription, provider }
  - response: { jobId, newNodeId }

POST /api/music/upload [P1]
  - body: multipart/form-data (file, projectId)
  - response: { musicId, url, duration }
  - 💡 음악 업로드/보관은 P1 기능, 최종 병합 반영은 P2에서 검토

GET /api/ai/jobs/{jobId}
  - response: { status, progress?, target, resultUrl?, error? }
  - 💡 완료/실패는 프로젝트 이벤트 WebSocket(8.10)으로도 전달됩니다.

POST /api/ai/jobs/{jobId}/requeue
  - description: 같은 jobId를 재실행 요청 (운영/특수 케이스용)
```

### 8.7 파일 API [P1]
```
POST   /api/files/presign   - Presigned URL 발급 (S3 직접 업로드)
POST   /api/files/complete  - 업로드 완료 등록 (메타데이터/소유자/용도 저장)
GET    /api/files/{id}      - 파일 정보 조회
DELETE /api/files/{id}      - 파일 삭제

💡 P1 기능: MVP에서는 AI 생성 이미지/영상만 사용, 외부 업로드는 P1에서 지원
```

### 8.8 타임라인/영상 편집 API
```
GET    /api/projects/{id}/timeline
  - 목적: 프로젝트 타임라인(확정된 클립) 조회
  - response(예): { items: [ { videoNodeId, sceneId, order } ] }

PUT    /api/projects/{id}/timeline
  - 목적: 클립 순서 변경 (드래그 앤 드롭)
  - body(예): { orderedVideoNodeIds: [101, 102, 103] }
  - 💡 정렬 대상 = `videoNodeId` (확정된 video 노드)

POST /api/projects/{id}/merge
  - body: { includeMusic: boolean } // P2 (음악 믹싱 반영)
  - response: { jobId }
  - 💡 merge 결과는 타임라인 순서를 기준으로 병합됩니다. (P1은 영상만 병합)

GET /api/projects/{id}/export
  - response: { downloadUrl }
```

### 8.9 WebRTC 시그널링 (WebSocket)
```
/ws/room/{roomId}?token=<JWT>
  - auth      : JWT 기반 인증(쿼리 파라미터 또는 쿠키 기반)
  - authorize : roomId(=projectId) 기준, **프로젝트 멤버만** join 가능
  - join      : 방 입장
  - leave     : 방 퇴장
  - offer     : SDP Offer 전송
  - answer    : SDP Answer 전송
  - candidate : ICE Candidate 전송
  - chat      : 텍스트 채팅

💡 MVP: 채팅 히스토리 저장 없음 (세션/연결 단위)
```

### 8.10 프로젝트 이벤트 (WebSocket)
```
/ws/projects/{projectId}?token=<JWT>
  - auth      : JWT 기반 인증(쿼리 파라미터 또는 쿠키 기반)
  - authorize : projectId 기준, **프로젝트 멤버만** 구독 가능

이벤트(예시):
  - job.done   : { jobId, target: { type: "NODE", id }, resultUrl }
  - job.failed : { jobId, target: { type: "NODE", id }, error }

💡 MVP 범위: AI Job 완료/실패 알림만 제공(진행률 푸시는 미제공)
```

---

## 9. 개발 일정 (6주)

| 주차 | 마일스톤 | 주요 작업 |
|------|----------|-----------|
| **W1** | 기획 완료 | PRD 확정, 와이어프레임, DB 설계, 환경 세팅 |
| **W2** | 기본 기능 | 인증 API, 프로젝트/씬 CRUD, 로그인/대시보드 UI |
| **W3** | AI 이미지 | Gemini API 연동, 스토리보드 생성 UI, Job Queue 구축 |
| **W4** | AI 영상 | Google Veo 3.1 API 연동, 영상 생성 UI, 진행률 표시 |
| **W5** | 협업 & 편집 | WebRTC 화상통화, 영상 병합(Server-side FFmpeg), 타임라인 UI |
| **W6** | 릴리즈 | 버그 수정, UI 폴리싱, 배포, 발표 준비 |

### 주차별 상세

```
Week 1: 기획 & 설계
├── PRD 확정
├── Figma 와이어프레임
├── DB 스키마 설계
├── API 명세 확정
└── 개발 환경 세팅 (Vue, Spring Boot, Docker)

Week 2: 기본 기능
├── [BE] 인증 API (회원가입, 로그인, JWT)
├── [BE] 프로젝트/씬 CRUD API
├── [FE] 로그인/회원가입 화면
├── [FE] 대시보드 (프로젝트 목록)
└── [FE] 프로젝트 상세 (씬 목록)

Week 3: AI 이미지 생성
├── [BE] Redis Job Queue 구축
├── [BE] Gemini API 연동 (이미지 생성)
├── [BE] Worker 프로세스 구현
├── [FE] 씬 편집 화면
├── [FE] 프롬프트 입력 → 이미지 생성 UI
└── [FE] 생성 상태 표시 (로딩, 완료, 실패)

Week 4: AI 영상 생성
├── [BE] Google Veo 3.1 API 연동
├── [BE] (P1) 오픈소스 모델 지원 검토
├── [FE] 이미지 선택 → 영상 생성 UI
├── [FE] 카메라 움직임 선택 UI
└── [FE] 영상 미리보기

Week 5: 협업 & 편집
├── [BE] WebRTC 시그널링 서버
├── [FE] 플로팅 협업 바 (화상통화 + 화면공유)
├── [FE] 타임라인 UI (씬별 클립 배치)
├── [BE] FFmpeg 기반 병합 Job (Worker)
├── [FE] 병합 완료/실패 처리 (WebSocket 이벤트 + 필요 시 Job 조회)
└── 통합 테스트

Week 6: 마무리 & 발표
├── 버그 수정 및 안정화
├── UI/UX 폴리싱
├── 배포 (AWS/Docker)
├── 발표 자료 준비
└── 데모 영상 제작 (1분 AI 영화)
```

---

## 10. 리스크 및 완화 방안

| 리스크 | 영향도 | 완화 방안 |
|--------|--------|-----------|
| AI API 비용/쿼터 | 높음 | 씬당 최대 10초, 전체 1분 제한, 일일 생성 횟수 제한, 해상도 제한 |
| AI 생성 지연 | 높음 | Job Queue + 진행률 UI, 사전 생성 샘플 준비 |
| Gemini/Veo API 장애 | 중간 | 오픈소스 모델 대안 (P1), 외부 업로드 대안 |
| WebRTC 구현 복잡도 | 중간 | 1:1 화상통화 먼저 구현, 필요시 OpenVidu 검토 |
| WebRTC 방화벽/NAT 이슈 | 중간 | 공개 TURN/STUN 서버 사용 (Google/Twilio), 기업망 테스트 필수 |
| 서버 병합 리소스/대기열 | 중간 | 영상 길이 1분 제한, Job Queue/동시 처리 제한, 우선순위/레이트리밋 |
| 브라우저 호환성 | 낮음 | Chrome/Edge 집중 지원 |

---

## 11. 성공 지표

### 11.1 MVP 완성 기준
- [ ] 프로젝트/씬 생성 및 관리 가능
- [ ] AI 시나리오 생성 동작
- [ ] Gemini로 스토리보드 이미지 생성 가능
- [ ] Google Veo 3.1로 Image-to-Video 생성 가능
- [ ] WebRTC 화상통화 + 화면공유 동작
- [ ] 씬별 영상 병합하여 최종 영상 생성 가능
- [ ] 전체 워크플로우가 끊김 없이 동작

### 11.2 발표/데모 목표
- **핵심 시연**: 워크플로우를 따라 **1분 내외의 AI 영화**를 라이브로 제작
- **데모 시나리오**:
  1. 프로젝트 생성 + AI 시나리오 생성
  2. 협업 바로 팀원과 브레인스토밍 (화상통화)
  3. 씬별 스토리보드 이미지 생성 (Gemini)
  4. 이미지 → AI 영상 생성 (Google Veo 3.1)
  5. 타임라인에서 씬 배치 + 병합
  6. 완성된 AI 영화 재생

### 11.3 데모 전략
- **사전 준비**: 데모 안정성을 위해 일부 씬은 미리 생성해둔 샘플 사용
- **라이브 생성**: 최소 1개 씬은 라이브로 이미지→영상 생성 시연
- **시간 관리**: AI 생성 대기 시간 동안 다른 기능 설명

---

## 12. 부록

### 12.1 용어 정의

| 용어 | 정의 |
|------|------|
| 프로젝트 | 하나의 AI 영화 작품 단위 |
| 씬 | 프로젝트 내 장면 단위 (스토리보드+영상+음악) |
| 스토리보드 | 씬의 구도를 나타내는 이미지 |
| 노드 | 씬 편집 캔버스의 구성 요소 (씬 헤더/마스터/그리드/샷/영상) |
| 마스터 이미지 | 씬의 기준이 되는 와이드 샷 이미지 (루트 노드) |
| 스토리보드 그리드 | 마스터 기반 여러 샷을 배치한 1장의 이미지 (2x2, 2x3 등) |
| 샷 | 그리드에서 추출한 개별 고품질 이미지 |
| 확정 (Confirm) | 영상 노드를 타임라인에 확정하는 액션 |
| Image-to-Video | 정지 이미지를 동영상으로 변환하는 AI 기술 |
| 플로팅 협업 바 | 모든 화면에서 유지되는 화상통화/화면공유 오버레이 |
| Veo 3.1 | Google Cloud의 Image-to-Video AI 모델 |

### 12.2 참고 자료
- [Google Gemini API](https://ai.google.dev/)
- [Google Veo API](https://cloud.google.com/vertex-ai/generative-ai/docs/video/overview)
- [Stable Video Diffusion](https://stability.ai/stable-video)
- [FFmpeg](https://ffmpeg.org/)
- [WebRTC 공식 문서](https://webrtc.org/)
- [Redis Streams](https://redis.io/docs/data-types/streams/)

---

## 13. 문서 이력

- v1.0 (2026-01-07): 초안 작성 (AI Movie Studio)
- v1.1 (2026-01-07): AI Shorts Studio로 피벗
- v1.2 (2026-01-08): 시장 조사, 모션 참조 AI, 디렉팅 패널 추가
- v2.0 (2026-01-12): **워크플로우 기반 AI 영화 제작 플랫폼으로 전면 재설계**
  - 촬영 기반 → 이미지 생성 기반으로 변경
  - Gemini API (스토리보드) + Runway/Luma (영상) 통합
  - WebRTC를 기획/브레인스토밍 단계로 이동
  - 타겟: 숏폼 크리에이터 → AI 영화 제작자
- v2.1 (2026-01-12): **MVP(P0) 범위 슬림화**
  - STEP3 샷 추출: AI 재생성만 P0, 직접 크롭은 P1으로 이동
  - 영상 생성: Runway 고정(P0), Luma/Kling 선택은 P1으로 이동
  - 기능 우선순위 표 및 UI 명세에 P0/P1 태그 명시
- v2.2 (2026-01-12): **UX 가이드라인 및 UI 개선**
  - 4.3 UX 가이드라인 섹션 신규 추가
  - 6.2.2 협업 컨텍스트 안내 카드 추가
  - 6.2.3 씬 편집 화면 개선
- v2.3 (2026-01-14): **노드 기반 UI + 플로팅 협업 바 전면 재설계**
  - 씬 편집: 스테퍼 UI → 노드 기반 캔버스 (Vue Flow)
  - 협업: 별도 화면 → 플로팅 바 (모든 화면에서 유지)
  - 노드 흐름: 세로 (위→아래), 분기는 좌우
  - 확정(Confirm) 개념: 영상 노드에서 타임라인에 확정
  - 레이아웃: 노드 캔버스 메인 + 사이드바 상세
  - DB 스키마: scene_nodes, node_edges 테이블 추가
  - P1 실시간 협업: Yjs/CRDT 상세 스펙 추가
- v2.4 (2026-01-14): **기술 스택 및 AI Provider 개편**
  - AI 영상 생성: Runway → **Google Veo 3.1** 단일화 (P0)
  - 오픈소스 모델 (Stable Video Diffusion 등) P1 옵션 추가
  - 샷 직접 크롭: P1 → P2로 우선순위 하향
  - OAuth 소셜 로그인: P1으로 명확히 분리
  - 기술 스택 보완: Redis Streams, GitHub Actions CI/CD, Prometheus+Grafana 모니터링, Swagger 추가
  - 영상 병합: Client(FFmpeg.wasm) → **Server-side FFmpeg(Worker Job)** 로 정리
  - 프로젝트 상세 화면: 배경음악 섹션 → 타임라인으로 이동, 팀 브레인스토밍 섹션 삭제
  - **일관성 개선**: 문서 전반 Runway→Veo 3.1 용어 통일, 협업 룸→플로팅 협업 바 통일
  - **API 보완**: 오브젝트 API (8.3), 노드 API (8.5), 타임라인 API (8.8), OAuth API 추가
  - **용어 정의 확장**: 노드, 마스터 이미지, 확정, 플로팅 협업 바, Veo 3.1 추가
- v2.5 (2026-01-14): **노드 시스템 상세 설계**
  - 4.2.6 전면 재작성: 노드 타입 5가지, 마인드맵 스타일 생성, 사이드바 UI 명세
  - 영상 노드: 시작 샷 기반(P0), 끝 샷(선택, P1) 트랜지션 영상 가능
  - 프롬프트 빌더: 한줄 설명 + 스타일/시간대/분위기 옵션 → 자동 생성
  - DB 스키마: start_shot_node_id, end_shot_node_id 컬럼 추가
  - 4.2.7 모션 설명 필드 추가
  - 프롬프트 생성/AI 개선: LLM 기반 API 추가(8.6) + Advanced(접기) UX 원칙 보강(4.3)
  - 재생성(regenerate): 덮어쓰기 대신 새 버전 노드 생성(히스토리 보존) 정책 명시
  - 타임라인 API: 정렬 대상(videoNodeId) 및 request body 명확화(8.8)
  - 다운로드: 노드 결과 다운로드 URL(`/api/nodes/{id}/export`) 및 최종 병합본 export 유지
  - 협업/이벤트: WebSocket 인증/권한 명시 + Job 완료/실패 프로젝트 이벤트 WS 추가(8.10)
  - 멤버 관리: 이메일 기반 초대 + 프로젝트 탈퇴 API 추가(Owner 탈퇴 정책 포함)
  - 인증: 비밀번호 재설정 API 추가
  - UI 정리: 코멘트/SNS 공유 범위 제거, 프로젝트 썸네일 규칙(첫 씬 Active Master) 추가
  - 동시성/제약: Active Master 유일성, shot당 confirm 유일성 보장 규칙 추가(7.5)
- v2.5.1 (2026-01-15): **용어 일반화 + AI 시나리오 생성 플로우 개선**
  - 캐릭터 시트 → **오브젝트 시트**로 변경 (캐릭터뿐만 아니라 소품 등 다양한 오브젝트 지원)
  - character_ids → object_ids로 변경
  - 등장 캐릭터 → 등장 오브젝트로 용어 통일
  - **AI 시나리오 생성 4단계 플로우**(4.2.4 전면 재작성):
    1. 기본 정보 입력 (장르/톤/씬 수 + 고급 옵션)
    2. AI 시나리오 프롬프트 생성 → 사용자 검토/승인
    3. 전체 줄거리 AI 생성
    4. 씬별 제목/스토리 생성
- v2.5.2 (2026-01-15): **노드 워크플로우 개선**
  - **씬 헤더 노드 추가**: 노드 5종(씬헤더/마스터/그리드/샷/영상), 씬 정보를 캔버스 상단에 표시
  - **모든 노드에 프롬프트 승인 플로우 적용**: 입력 → AI 프롬프트 생성 → 검토/수정 → 승인 → 결과 생성
  - 마스터 노드: 씬 스토리 자동 참조, 프롬프트 검토 UI 추가
  - 그리드 노드: 구도 힌트 입력, 프롬프트 검토/승인 추가
  - 샷 노드: 표정/디테일 조정, 프롬프트 생성/검토/승인 추가
  - 영상 노드: 카메라/모션 설정 후 프롬프트 검토/승인 추가
- v2.5.3 (2026-01-15): **2단계 병합 구조**
  - 영상 편집(4.2.9) 전면 재작성
  - **씬 타임라인**: 씬 편집 화면 하단, 씬 내 클립들 → 씬 영상 병합
  - **프로젝트 타임라인**: 별도 화면, 모든 씬 영상들 → 최종 영화 병합
  - 씬별 미리보기 + 전체 미리보기 기능 명시
  - 음악 기능 범위 정리: **P1 업로드/보관**, **P2 믹싱/병합 반영**
  - CI/CD: Jenkins로 통일
  - 상태값 정리: Job status(PENDING/RUNNING/SUCCEEDED/FAILED), Node status(PENDING/RUNNING/SUCCEEDED/FAILED)
  - 탭명: Characters → Objects
